def load_text(label, **kwargs):
    TEXT_FILE_NAME = 'message_texts.txt'
    text = 'Заглушка (напишите функцию!)'
    # here should the text loader be written
    return text


def form_keyboard(user, state):
    pass
